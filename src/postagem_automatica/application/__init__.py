"""Camada de aplicação: caso de uso e ports (interfaces).

Depende apenas do domínio — nunca de SDKs concretos (Dependency Inversion:
quem implementa as ports é a camada de infraestrutura, que aponta para cá).
"""
